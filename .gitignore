# Python
__pycache__/
*.py[cod]
*.egg-info/

# PyInstaller build output
build/
dist/
*.manifest
.buildenv/

# Runtime data (generated at run time — should not be versioned)
logs/
output/
Output_Production/
received/
spool/
manual/
*_log_state.json

# Real runtime configs — contain secrets (ENC: passwords) and host specifics.
# Commit the *_config.template.json files instead and copy them on each host.
*_config.json
*_config.json.bak
*.tmp

# Secrets / environment
lane_check_status.env
.env

# OS junk
.DS_Store
